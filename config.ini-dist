[database]
;Database to be used by AUS applications, in URI format
;dburi=sqlite:////var/www/aus/update.db
;dburi=mysql://user:password@host/database

[paths]
;Virtualenv that the WSGI scripts should load before importing the application.
virtualenv=/var/aus/virtualenv
